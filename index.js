'use strict';

const e = React.createElement;

class Message extends React.Component {
    constructor(props) {
        super(props);
        this.state = { msg: "BEST VETS" };
    }
    render() {
        return e(

        );
    }
}

const domContainer = document.querySelector('#app');
ReactDOM.render(, domContainer);
